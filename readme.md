# Shop Checker Bot

Проєкт для моніторингу каталогу монет НБУ та сповіщень у Telegram.
Проєкт відкриває каталог у Chromium через Playwright і надсилає підсумок у Telegram,
якщо є відмінності від фіксованого знімка. Перша перевірка виконується одразу,
наступні — після паузи `CHECK_INTERVAL_MS` у `variables.ts` (типово 60 секунд).
Пауза починається після завершення перевірки; перевірки не перекриваються.
Після помилки програма повторить спробу через цей самий інтервал.

## Запуск

Потрібен Node.js 24 або новіший і pnpm 11.24.0. Перед першим запуском виконайте `pnpm install`.
Node.js запускає TypeScript напряму, без окремої збірки та перевірки типів.

Створіть `.env` за прикладом `.env.example` і заповніть `TELEGRAM_BOT_TOKEN`
та `TELEGRAM_CHAT_ID`. Команди запуску автоматично читають цей файл.
`.env` виключено з Git.

Перед першим запуском встановіть браузер:

```sh
pnpm exec playwright install chromium
```

```sh
pnpm start
```

Автоматичний перезапуск після змін:

```sh
pnpm dev
```

Перевірка типів (TypeScript і `@types/node` використовуються лише для розробки):

```sh
pnpm typecheck
```

Точка входу — `run.ts`, головна функція — `main()`.
Вона викликає `openCatalog()`, що відкриває каталог у видимому вікні браузера.
Браузер закривається після кожної перевірки. Щоб зупинити моніторинг, натисніть Ctrl+C.

`products.json` — фіксований знімок каталогу. Кожен запуск порівнює сайт
із цим файлом за ID та показує додані, відсутні товари й зміни назви або URL.
Програма лише читає цей файл і ніколи не оновлює його автоматично.
Ціна та наявність поки не порівнюються.
Якщо відмінності зберігаються, наступний запуск знову надішле повідомлення.
